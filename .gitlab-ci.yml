stages:
  - buildRC

variables:
  LC_ALL: "en_US.UTF-8"
  LANG: "en_US.UTF-8"

before_script:
  - export ANDROID_HOME="/sdk"
#  - gem install bundler
#  - bundle install

buildRC:
  dependencies: []
  stage: buildRC
  artifacts:
    paths:
      - fastlane/screenshots
      - fastlane/logs
  script:
    - fastlane buildRC
  tags:
    - android