.macos_saas_runners:
  tags:
    - local-macos-intel-i7
  image: macos-12.6.5

stages:
  - buildRC

variables:
  LC_ALL: "en_US.UTF-8"
  LANG: "en_US.UTF-8"

# before_script:
#   - gem install bundler
#   - bundle install

buildRC:
  dependencies: []
  stage: buildRC
  artifacts:
    paths:
      - fastlane/screenshots
      - fastlane/logs
  script:
    - fastlane buildRC
  tags:
    - android